- 采用 Hugo 静态站点生成器架构，以 `config.toml` 为核心配置，指定 `hugo-bearblog` 主题。
- 内容层由根目录的 `index.html`（手动维护的下载页）和 `public/` 目录（Hugo 生成的发布页）组成。
- 集成第三方服务：通过 `CNAME` 绑定域名，嵌入 Crisp 聊天脚本提供实时客服支持。
- 资源管理：`public/css/custom.css` 提供样式覆盖，`bak/` 目录用于版本备份。